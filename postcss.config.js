// PostCSS configuration for CSS processing
export default {
  // PostCSS plugins to transform CSS
  plugins: {
    // Tailwind CSS plugin for utility-first CSS framework
    tailwindcss: {},
    // Autoprefixer adds vendor prefixes for browser compatibility
    autoprefixer: {},
  },
};
